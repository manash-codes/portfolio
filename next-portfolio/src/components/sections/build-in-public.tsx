import { profile } from "@/src/constants/portfolio";
import { SectionHeading } from "@/src/components/ui/section-heading";

const notes = ["Building reliable AI features means treating evaluation as a product surface.", "A fast interface starts with a smaller question: what can the browser avoid doing?", "Architecture earns its keep when it makes the next safe change less expensive."];

export function BuildInPublic() {
  return <section id="public" className="section public-section"><SectionHeading eyebrow="05 / Build in public" title="The work continues between releases." copy="Follow the public trail: source code, notes, and the practices currently being sharpened." /><div className="public-grid"><article className="contribution-card"><div><p className="eyebrow">GitHub activity</p><h3>Consistent, public progress.</h3><p>Explore current contributions and recent repositories directly on GitHub.</p><a className="text-link" href={profile.github} target="_blank" rel="noreferrer">Open GitHub profile <span>↗</span></a></div><div className="contribution-grid" aria-label="Link to live GitHub contribution graph">{Array.from({ length: 70 }, (_, index) => <i key={index} className={`level-${(index * 7 + index % 3) % 5}`} />)}</div></article><div className="notes"><p className="eyebrow">Technical notes</p>{notes.map((note, index) => <article key={note}><span>0{index + 1}</span><p>{note}</p></article>)}<a className="text-link" href={profile.linkedin} target="_blank" rel="noreferrer">More writing on LinkedIn <span>↗</span></a></div><article className="roadmap"><p className="eyebrow">Learning roadmap</p><h3>Deepening the parts of the stack that compound.</h3><ol><li>Agent evaluation & production observability</li><li>Distributed systems design</li><li>Product-focused platform engineering</li></ol></article></div></section>;
}
